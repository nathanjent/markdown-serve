# markdown-serve
Create a simple HTTP host to view markdown files in the browser.

After making this work I came to realize that checkboxes are not an official markdown feature. Github and some other applications have added support for checkboxes but I'll have to either find a library that supports it or add support myself.
